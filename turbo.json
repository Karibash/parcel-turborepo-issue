{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build:parcel": {
      "outputs": [
        "dist/**"
      ]
    },
    "build:tsup": {
      "outputs": [
        "dist/**"
      ]
    }
  }
}
